class Avo::Resources::Fund < Avo::BaseResource
  # self.icon = "tabler/outline/users"
  # self.avatar = {
  #   source: :avatar
  # }
  # self.includes = []
  # self.attachments = []
  # self.search = {
  #   query: -> { query.ransack(id_eq: q, m: "or").result(distinct: false) }
  # }

  def fields
    field :id, as: :id
    # field :avatar, as: :avatar
    field :amount, as: :number
    field :detail, as: :text
    field :is_deposit, as: :boolean
    field :transaction_date, as: :date
  end
end
